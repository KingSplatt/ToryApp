# Environment Variables for Production

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database_name

# OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret

# CORS Configuration (comma-separated URLs)
# Add your Netlify domain here, for example:
# CORS_ORIGINS=https://your-netlify-app.netlify.app,https://toryappfront.netlify.app,http://localhost:5173
CORS_ORIGINS=https://toryappfront.netlify.app,http://localhost:5173,http://localhost:5174

# ASP.NET Environment
ASPNETCORE_ENVIRONMENT=Production
# Render Port (automatically set by Render)
PORT=10000
